# Example MIDI

- **`demo_scale.mid`** — Short C major scale on GM piano (generated by `scripts/write_example_midi.py`, original sequence).

Regenerate after edits:

```bash
python scripts/write_example_midi.py
```
