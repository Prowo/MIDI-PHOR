# Example MIDI

- **`demo_abba.mid`** — Short multi-track demo labeled for the HF Space (melody / pad / bass style; generated by `scripts/write_example_midi.py`). Not affiliated with ABBA; name is for recognizable layout in the UI.

Regenerate after edits:

```bash
python scripts/write_example_midi.py
```
